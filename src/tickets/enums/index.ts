export enum Priority {
  VERY_LOW = 'very low',
  LOW = 'low',
  NORMAL = 'normal',
  HIGH = 'high',
  VERY_HIGH = 'very high',
}

export enum Status {
  TODO = 'todo',
  DESIGN = 'design',
  IN_PROGRESS = 'in progress',
}
